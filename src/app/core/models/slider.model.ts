export interface ISlider {
    id: number;
    imageUrl: string;
    title?: string;
    description?: string;
  }
  export interface ICreateSlider {
    imageUrl: string;
    title?: string;
    description?: string;
  }