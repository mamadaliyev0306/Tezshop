export interface IReview {
    author: string;   // Sharhni yozgan foydalanuvchi nomi
    date: string;     // Sharh sanasi (ISO formatda bo'lishi mumkin)
    rating: number;   // Reyting (1 dan 5 gacha)
    comment: string;  // Sharh matni
  }
  